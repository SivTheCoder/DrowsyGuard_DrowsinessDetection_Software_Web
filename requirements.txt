streamlit==1.40.1  # Matches local version, available for 3.10
streamlit-webrtc==0.48.2  # Matches local version, avoids version mismatch
opencv-contrib-python==4.11.0.86  # Matches mediapipe's dependency, replaces opencv-python-headless
numpy==1.24.3  # Matches local version, compatible with mediapipe
mediapipe==0.10.11  # Matches local version